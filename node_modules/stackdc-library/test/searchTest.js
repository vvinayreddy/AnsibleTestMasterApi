var searchClient = require('../').searchClient;


var searchClientObj = new searchClient({
  host: 'zoetis-dev.stackdc.io:9200',
  log: 'error',
  attributes: ['title', 'link', 'user','created'],
  index: 'cloud-stream-store',

})

 searchClientObj.searchAll('zoetis', function (error, response, status) {
    if (error) {
        console.log("search error: " + error)
    } else {
        console.log("--- Response ---");
        console.log(response);
        console.log("--- Hits ---");
        response.hits.hits.forEach(function (hit) {
            console.log(hit._type,hit._id);
            if(hit._id==='58ad3344f8d13ca071e8df9d')
            console.log('hit details',hit)
        })
    }
}); 

var fields = [
  {
    text: "bayer"
  },
  {
    field: "category",
    values: []
  },
  
  {
    field:"topic.topicName",
    values: []
  }

]

// searchClientObj.searchFields('cloud-stream-store',undefined,fields
// , function (error, response, status) {
//   if (error) {
//     console.log("search error: " + error)
//   } else {
//     console.log("--- Response ---");
//     console.log(response);
//     console.log("--- Hits ---");
//     // response.hits.hits.forEach(function (hit) {
//     //   console.log(hit);
//     // })
//   }
// });

// searchClientObj.search('zoetis', {
// //  indexType: 'Article',
//   fields: ['title'],
//  // attributes:['title','user','link']
// }, function (error, response, status) {
//   if (error) {
//     console.log("search error: " + error)
//   } else {
//     console.log("--- Response ---");
//     console.log(response);
//     console.log("--- Hits ---");
//     // response.hits.hits.forEach(function (hit) {
//     //   console.log(hit);
//     // })
//   }
// });


/* searchClientObj.searchField('stream-store','Article',['articleTitle'],'Docker',function (error, response, status) {
  if (error) {
    console.log("search error: " + error)
  } else {
    console.log("--- Response ---");
    console.log(response);
    console.log("--- Hits ---");
    response.hits.hits.forEach(function (hit) {
      console.log(hit);
    })
  }
}); */