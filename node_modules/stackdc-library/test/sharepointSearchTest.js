 var sharepointLib = require('../').sharepointClient;

var sharepointClient = new sharepointLib();

var config = require('local/test_data.js').sharepointSearchTest


sharepointClient.init(config);
sharepointClient.search('zoetis',function(err,data){
  if(err)
    console.log('error',err);
  else
  console.log('data:',data);  
})
